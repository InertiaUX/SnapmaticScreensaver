# apps/linux

Linux builds use the shared portable launcher in [`apps/desktop`](../desktop/).

```bash
./apps/desktop/build.sh
# → dist/desktop/Snapmatic-Screensaver-*-linux-amd64.zip
```

Needs a Chromium-based browser for kiosk/app mode. See [docs/platforms.md](../../docs/platforms.md).
